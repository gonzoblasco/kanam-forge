/**
 * adorable-vm.ts
 *
 * Local implementation. Creates a Docker container per project instead of a
 * Freestyle cloud VM. Re-exports from ./docker/docker-vm.
 */

export * from "./docker/docker-vm";
