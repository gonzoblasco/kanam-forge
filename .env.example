# Kanam Forge - environment variables
# Copy to .env.local and fill in. Alternatively, set the API key from the
# in-app settings dialog (ApiKeyGate) which stores it in a cookie.

# --- LLM Provider ---
# Choose which provider to use: "openai" (default) or "anthropic"
LLM_PROVIDER=openai

# OpenAI API key (used if LLM_PROVIDER=openai)
OPENAI_API_KEY=

# Anthropic API key (used if LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=

# --- Local Docker sandbox (Fase 1) ---
# Directory where project volumes are stored on the host
KANAM_FORGE_DATA_DIR=~/.kanam-forge/projects

# Docker socket or API endpoint for the container orchestrator
DOCKER_HOST=unix:///var/run/docker.sock
